# ИИ-анализ диалогов (пакет crmai). Ставится отдельно от парсера tghunter:
#   pip install -r requirements-crmai.txt

fastapi>=0.110
uvicorn[standard]>=0.27
APScheduler>=3.10,<4.0
anthropic>=0.40
requests>=2.31
python-dotenv>=1.0

# для тестов
# pytest>=8.0
